Here are the summary results of analysis of the model instance with not scaled
parameters (model instance is in ..../cn_ptl/prototype/gams_run2a).
The results illustrate numerical problems, see the *png Figs generated from
../985/tradeoffs.numbers, which contains also summary of results of analysis 985
(which was made on the instance with scaled parameters).

The *gdx file are on the MCMA server (in /t/marek/cn_ptl dir); they can be
also extracted (one by one) through the MCMA app.
